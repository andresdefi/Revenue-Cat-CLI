version: "2"

linters:
  enable:
    - errcheck
    - govet
    - gosimple
    - ineffassign
    - staticcheck
    - unused
    - misspell

formatters:
  enable:
    - gofmt

exclusions:
  paths:
    - vendor
  rules:
    - path: _test\.go
      linters:
        - errcheck
